<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.alpha10">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".PercursosIndivid">
        </activity>
        <activity android:name=".PercursoCategoria">
        </activity>
        <activity android:name=".PercursosCriar">
        </activity>
        <activity
                android:name=".PercursosMain"
                android:label="@string/title_activity_percursos_main">
        </activity>
        <activity
                android:name=".ProfileMain"
                android:label="@string/title_activity_profile_main">
        </activity>
        <activity android:name=".SignIn">
        </activity>
        <activity android:name=".LogIn">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>